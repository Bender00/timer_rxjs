import React from 'react';
import ReactDom from 'react-dom';
import Proect from './scripts/proect';

ReactDom.render(
    <React.StrictMode>
        < Proect />
    </React.StrictMode>,
    document.getElementById("root")
);